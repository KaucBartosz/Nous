// src/modules/recaptcha.js
// reCAPTCHA removed due to budget constraints.
// Stubs preserved to prevent import errors.

export async function loadRecaptcha() { return false; }
export async function getRecaptchaToken() { return null; }
export function isRecaptchaEnabled() { return false; }
